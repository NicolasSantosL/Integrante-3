   f					RELATÓRIO

Primeira parte (Carlos)

Primeiro é criado uma pasta e nesta pasta crie um doc de txt, nesse txt altere dentro dele, após entre no git-bash e de git init para inicializar um repositório então criamos a branch main e demos commit duas vezes criando então a versão 1 e a versão 2.
Entramos no repositório criado pelo usuario 1 com esse comando: “git remote add origin https://github.com/carloseduardo04/Nirvana02.git” . Logo então para enviar pro meu github usamos o comando ”git push -u origin main” só que deu erro porque um usuário já estava conectado, aparecia que o usuário 1 não tinha permissão para mandar o repositório, sendo barrado por um usuário aleatório, para consertarmos fomos nas credenciais do windows e retiramos o usuário do git bash.

Segunda parte (Bruno)

Na parte do arquivo Blog, foi dado “git init” para a inicialização do arquivo, logo após, foi criado uma branch e um arquivo .txt que algumas informações foram inseridas, foi dado o commit do arquivo, e novamente, foi alterado o arquivo .txt para a versão 1.1, e foi feito a mesma coisa novamente, só que foi dado o commit para a versão 1.2. Na hora de dar o comando “push”, o arquivo não foi transferido para o GitHub por causa de um problema que ocorreu. O problema aconteceu porque eu tentei dar Push no repositório para outra conta que não era a minha, mas após perceber esse erro, o problema foi solucionado e o envio foi concluído.

Terceira parte (Nicolas)

Na feature “Relatório”, consequentemente foi criado uma branch chamada “Relatório”, o mesmo nome do arquivo. Após dar o “git init” e criar a branch, foi criado um arquivo .txt chamado “Integrante-3”, dentro do arquivo .txt, alterei o arquivo, realizei o commit e fiz a mesma coisa, alterando o arquivo mais duas vezes (totalizando 3 modificações), commitei os arquivos alterados, e por fim, fiz o comando “git push” para enviar o arquivo da atualização final para o GitHub.
